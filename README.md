# CareerQuest SG

Run:

```bash
pip install pandas networkx
python scripts/load_seed_data.py
```

This loads the CSV files into a local SQLite database and runs two first milestone queries:

1. What skills does Data Analyst need?
2. What path from Administrative Executive to Data Analyst?
